Créez un programme qui transforme une heure affichée en format 12h en une heure affichée au format 24h.
Attention : midi et minuit
il faudra gérer les différentes erreurs d'arguments

function convertirHeure(heure12h) {
  // Extraire l'heure, les minutes et AM/PM de l'heure au format 12 heures
  let heure = parseInt(heure12h.split(":")[0]);
  let minutes = parseInt(heure12h.split(":")[1]);
  let estAM = heure12h.indexOf("AM") > -1;
  let estPM = heure12h.indexOf("PM") > -1;
  
  // Vérifier si l'heure est minuit ou midi
  if (heure === 12 && estAM) {
    heure = 0;
  } else if (heure !== 12 && estPM) {
    heure += 12;
  }
  
  // Formater l'heure au format 24 heures
  let heure24h = (heure < 10 ? "0" : "") + heure + ":" + (minutes < 10 ? "0" : "") + minutes;
  
  return heure24h;
}

// Récupérer l'heure à convertir à partir du terminal
const heure12h = process.argv[2];

// Convertir l'heure
const heure24h = convertirHeure(heure12h);

// Afficher le résultat
console.log(`L'heure ${heure12h} en format 24 heures est : ${heure24h}`);
